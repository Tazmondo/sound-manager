local Animations = require(script.Animations)
local Decals = require(script.Decals)
local Sounds = require(script.Sounds)
local Effects = script.Effects
local Models = script.Models

return {
	Animations = Animations,
	Decals = Decals,
	Sounds = Sounds,
	Effects = Effects,
	Models = Models,
}
